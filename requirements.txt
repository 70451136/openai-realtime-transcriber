# 核心依赖
pyaudio
pyqt5
numpy
websocket-client
openai
requests

# 音频处理
wave
pydub

# 可视化（可选）
pyqtgraph

# 系统和工具库
# 以下是Python标准库，无需安装：
# sys, threading, time, queue, tempfile, os, asyncio, json
# base64, collections, concurrent.futures, traceback, logging, difflib
